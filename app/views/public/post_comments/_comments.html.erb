<div class="col-8 mt-5 offset-2">
  <h3 class="main-font"><i class="fas fa-comments "></i>Comment</h3>
  <div class="table-responsive">
  <table class="table table-hover mt-3" >
    <tbody>
      <% comments.each do |post_comment| %>
      <tr>
        <%# ユーザー %>
        <td>
          <%= link_to  listener_path(post_comment.listener), class: "text-dark" do %>
          <div><%= attachment_image_tag post_comment.listener,:profile_image ,size:"50x50", fallback: "no_image.jpg",class:"mt-3 rounded-circle" %></div>
          <div>&ensp;<%= post_comment.listener.name %></div>
          <% end %>
        </td>
        <%# コメント %>
        <td class="pt-4"><%= post_comment.comment %></td>
        <%# コメント編集・削除 %>
        <% if post_comment.listener == current_listener %>
        <td class="">
          <div class="btn d-block" style="width: 70px; ">
            <%= link_to edit_post_post_comment_path(post_comment.post,post_comment),method: :get, class: "text-info" do %>
              <i class="fas fa-edit">Edit</i>
            <% end %>
          </div>
          <div class="btn d-block" style="width: 70px; ">
            <%= link_to post_post_comment_path(post_comment.post,post_comment),method: :delete,'data-confirm' => "Do you really want to delete?", class: "text-danger",remote: true do %>
              <i class="fas fa-trash-alt">Delete</i>
            <% end %>
          </div>
          </td>
        <% end %>
      </tr>
      <% end %>
    </tbody>
  </table>
  </div>
  <%# コメント投稿フォーム %>
  <div >
    <%= form_with(model:[post, post_comment_n], local: false) do |f| %>
    <%# コメント欄 %>
    <div>
      <%= f.text_area :comment, rows:'5',placeholder: "Comment" ,class: "form-control" %>
    </div>
    <%# 送信ボタン %>
    <button class="btn btn-info mt-4" style="width:100px">POST
      <% f.submit %>
    </button>
    <% end %>
  </div>
</div>





